/**
 * Contains the input generators and the fake collaborators the benchmarks share.
 * <p>
 * Nothing in this package is measured. It exists so every benchmark builds its input in a setup
 * method and so no benchmark has to start a Minestom server to resolve a block or a biome.
 * </p>
 *
 * @author TheMeinerLP
 * @version 1.0.0
 * @since 0.1.0
 */
package net.onelitefeather.falco.benchmark.support;
